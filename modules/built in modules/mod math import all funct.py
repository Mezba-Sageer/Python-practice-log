#import of all functions inside a module
#'*' is used at the end of import to import all functions inside a particular modules
# so that the functions inside a module need not be called separately after import statement
from math import*
print(pi)
print(sqrt(36))

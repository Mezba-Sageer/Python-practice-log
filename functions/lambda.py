#lambda is a key word
sum = lambda a,b,c: a+b+c
#sum is similar to function name,lambda is a keyword,
# a,b and c is the variables that will be used and ':' represents return and
# after full colon the operation to be performed is passed
print(sum(2,4,6))

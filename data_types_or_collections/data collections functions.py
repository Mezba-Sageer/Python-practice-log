#syntax for returning the index of an element
#variable_name(list/tuple/dic/array/set).index("element")

#syntax to print an element using index num
#print(variable_name[index value])

#syntax to deleting a particular element in a list/tuple/array/set/dic
#del variable_name[index no.of the element]

#syntax to adding an element into a tuple,set,dic,array,list:
#variable_name.append(input("Enter a name: "))

#syntax for sorting a list element : only applicable to lists
#variable_name.sort()

#syntax for using sorted() on any iterables including string,list,tuple,set,dictionary:
#variable name=sorted(iterable to sort)
#print(variable name)
#here we assign the sorted values into another variable so the original data remains untouched
#this sorting method is used in all other data types other than list
#print(sorted(names, key=len))  key=... → Custom sort logic (like sorting strings by length)


